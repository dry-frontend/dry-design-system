import styled from 'styled-components';

const PlainButton = styled.button`
  appearance: none;
  border-radius: unset;
  border: unset;
  outline: unset;
  box-shadow: unset;
  background: unset;

  font-size: 16px;
  cursor: pointer;
`;

export default PlainButton;
