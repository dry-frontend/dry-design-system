import styled from 'styled-components';
import { Container, Input } from '../Input.styled';

export const CvcContainer = styled(Container)`
  width: 85px;
`;

export const CvcInput = styled(Input)`
  width: 100%;
  text-align: center;
`;
